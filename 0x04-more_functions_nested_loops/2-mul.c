#include "main.h"

/**
 * mul -  multiplies two integers.
 *
 * @a: checks first input of function
 * @b: checks second input of function
 *
 * Return: returns 'a' * 'b'
*/

int mul(int a, int b)
{
	return (a * b);
}
