#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string.
 *
 * @s: checks input of function
 *
 * Return: returns length of a srting 's'
*/

int _strlen(char *s)
{
	return (strlen(s));
}
