#include "oper.h"

/**
 * mod - return the moduls of 2 numbers.
 *
 * @num1: checks input of function
 * @num2: checks input of function
 *
 * Return: returns the moduls of num1 and num2.
*/

int mod(int num1, int num2)
{
	return (num1 % num2);
}
