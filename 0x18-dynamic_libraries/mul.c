#include "oper.h"

/**
 * mul - return the  multpliaction of 2 numbers.
 *
 * @num1: checks input of function
 * @num2: checks input of function
 *
 * Return: returns the multpliaction of num1 and num2.
*/

int mul(int num1, int num2)
{
	return (num1 * num2);
}
