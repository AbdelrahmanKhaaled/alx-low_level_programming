#include "oper.h"

/**
 * add - return the summation of 2 numbers.
 *
 * @num1: checks input of function
 * @num2: checks input of function
 *
 * Return: returns the summation of num1 and num2.
*/

int add(int num1, int num2)
{
	return (num1 + num2);
}
